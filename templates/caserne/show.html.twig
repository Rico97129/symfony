{% extends 'base.html.twig' %}

{% block title %}Caserne{% endblock %}

{% block body %}
    <h1>Caserne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caserne.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ caserne.libelle }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ caserne.adresse }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ caserne.cp }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ caserne.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_caserne_index') }}">back to list</a>

    <a href="{{ path('app_caserne_edit', {'id': caserne.id}) }}">edit</a>

    {{ include('caserne/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_sapeur_pompier_delete', {'id': sapeur_pompier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sapeur_pompier.id) }}">
    <button class="btn">Delete</button>
</form>
